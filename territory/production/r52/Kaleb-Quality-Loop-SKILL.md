---
name: kaleb-quality-loop
description: Plan, build, independently critique, fix, verify and checkpoint substantive tasks for Kaleb across code, 3D, maps, media, writing and research. Use visible evidence-based progress. Keep simple questions lightweight.
---

Follow Docs/AI/UniversalWorkStandard.md when it is available in the repository.
Recover the actual requirements, approvals and current state. Make an evidence-backed
acceptance checklist, dependency order and next executable step. Implement useful
work without endless replanning. Keep the whole requested scope intact.

Use a genuinely separate critic through an available agent runtime; record the real
run reference. Never relabel self-review or a validator as an independent agent.
Inspect actual screenshots from multiple relevant angles/states for visuals, motion
sequences for animation, and appropriate tests/sources/listening for other tasks.
Unknown or unreviewed areas cannot receive invented passing scores.

Score 0-10. PASS strictly >9.0, with 10/10 the target. Every mandatory requirement and
required evidence must pass, with no blocking/major defect. Averages cannot mask
failed required areas. Iterate implement -> test/capture -> critique -> fix ->
retest/review while meaningful execution remains possible. Keep blocked work blocked.

Show concise progress commentary during substantial execution, generally every
15 seconds or 2-3 calls when possible, subject to tool restrictions. Use actual
verified/required counts, named scope and stage, never fake time-driven percentages.
Save the plan, current revision, evidence, defects, fixes, blocker and next step.
This skill is a reusable definition, not an app-level progress widget or proof of
cross-chat installation, and it cannot control already-running conversations.

## Territory-card R44 family standard — 2026-09-12

For every territory-card task, read the active `ACTIVE-SKILLS.json` and the exact canonical files under `/Skills/Territory Cards/`. Apply `references/New-Designed-Card-Family-Contract.md` as a hard prerequisite before builder/critic work. Do not select a same-named stale Library copy outside the active path.

A territory-card pass requires: canonical shell validator PASS, complete `card_family_review`, New Designed visual regression PASS, all geography/source/coverage gates, and the active critic threshold. A technically valid card that does not match the canonical family remains a failure. Territory 254 may never override the A33/60AB outer shell.


An exact 9.0 territory score is a failure. Builder/critic text or historical batch records that say “9.0 inclusive” are superseded by this active R44 rule.


## Territory R45 label-placement override — 2026-09-12

For territory-card work, the builder and critic must both apply the canonical label decision tree independently. A label pass requires road attachment and composition, not just collision freedom. Review/fix in this order: direct road-following -> curved road-following -> clearer same-road position -> nearby attached-arrow callout -> dedicated detail. Any avoidable floating/overhung label, premature or distant callout, detached/wrong arrow, or cluttered callout neighborhood keeps the task in repair status regardless of numeric averages. Require the exact-artifact label decision ledger and its validator before approval.


## Territory R46 automatic execution and reviewer parity — 2026-09-12

For **every** territory-card task in regular Chat or Work, automatically read the
active `/Skills/Territory Cards/ACTIVE-SKILLS.json` and the exact active territory
contracts before substantive territory work. The user does not need to say “use the
skills.” If the active package is inaccessible, recover it through an authorized
Project/repository/package route or block the artifact workflow rather than proceeding
without the territory standards.

For every created or changed territory artifact, the **mandatory internal reviewer**
runs automatically after builder/deterministic gates and again on the exact final
saved/delivered PDF. It must operate in critic-only mode and apply the full Territory
Card Critic contract with identical evidence, hard vetoes, report schema and score
minimum. Internal means reviewer provenance only; it never means a lighter review.
Record `critic_independent:false`. Run a genuinely separate critic too whenever a
callable runtime exists or independence is required. Never average disagreement.

Any byte change invalidates prior review evidence. A user-reported defect reopens
approval. Read `references/Territory-Automatic-Execution-Contract.md` and require
`reviewer_parity_review` plus its validator before release.

## Territory R47 locked-template/style-token override - 2026-09-12
For territory-card creation/conversion/full rebuild in Chat or Work, the R46 automatic chain must load the active R48 locked template contract and style tokens before build. New output must originate from the locked renderer, retain exact token provenance, and pass both the strict token validator and canonical family validator before mandatory internal critic review.

Do not call a new/rebuilt card complete when its shell was manually reconstructed, its token provenance is missing, its territory ID/locality uses an unapproved layout variant, or its final PDF differs from the reviewed token-bound artifact. Bounded revisions of already approved exact cards remain mask-preserved rather than automatically rebased.


## Territory R48 identity/naming override — 2026-09-12
For every territory-card task, keep master-map/source identity separate from card identity. Automatically apply `Territory-Identity-Naming-Contract.md` in Chat and Work. No `R-###-X` source notation may leak onto the card face or production filename. Require the R48 identity validator and exact delivered filename check in both builder and mandatory internal-review lanes.


## Territory R49 source-truth + pre-critic override — 2026-09-12
For territory cards, add a mandatory quality stage before formal critic: **source truth -> endpoint/topology trace -> quick family preflight -> critic**. New/full-rebuild/materially redrawn geography must be grounded in raw assignment evidence plus an independent current topology source. Builder-generated reconstructions, prior critic prose and prior scores cannot be the sole proof.

For residential/neighborhood redraws, the exact candidate must be visually compared side-by-side with Territory 273 before critic dispatch. Obvious geography or family defects are builder/preflight failures and must be repaired early instead of consuming critic rounds. The formal critic/internal reviewer must independently re-open raw sources and may not copy the builder's topology ledger as proof. Territory 269 R48 is permanently negative after user rejection; its old pass scores are invalid evidence.

## Territory R50 label-completeness / clutter override — 2026-09-13
For every territory artifact that creates/redraws/repairs labels, load `Territory-Label-Completeness-Preflight-Contract.md` automatically. The executable chain is now:
`raw source lock -> visible road/name inventory -> topology trace -> render measurable PDF -> freeze preflight hash -> label completeness/contact/clutter preflight -> 273 family preflight when residential -> formal critic/internal review -> repair -> fresh exact-byte final review`.

The critic/internal reviewer must independently re-inventory visible roads and labels; a builder ledger or prior PASS cannot be reused as proof. Any missing named public street/cul-de-sac label, label on/touching a road, avoidable cluster, malformed/premature callout, or unverified private/unnamed omission reopens the artifact and blocks approval regardless of averages.

Generated/raster map concepts are drafts only, never release candidates. Target 10/10 remains; pass is strictly >9.0 only after every R50 zero-defect count and visual check passes.



## Territory R51 PDF-delivery / measured-label override — 2026-09-13
For territory cards, completed output means the canonical PDF, never an image-only preview. Before formal review, measure label-to-road spacing on the exact PDF and compare disputed/outlier labels with a same-card median baseline derived from at least three clearly good labels. The critic/internal reviewer must independently remeasure the disputed labels and reference sample. Long/complex roads may require multiple navigation labels; each repeated placement must improve navigation without creating contact or clutter. Require the R51 delivery and metric validators in addition to every R50/R49/R48 gate.

## Territory R52 semantic-target and whole-label override — 2026-09-13
Automatically load `Territory-Segment-Role-Whole-Label-Contract.md`. Before layout, lock each label's navigation purpose and exact source segment. Before critic dispatch and on exact saved bytes, run role/target, whole-label ending and actual-font-spacing checks. The reviewer separately verifies semantic navigation meaning and start-to-suffix contour/legibility. A technically clean label on the wrong same-named road portion fails; zero glyph overlap cannot excuse visibly cramped words. Preserve successful repeats and approved card bytes; record positive component and negative rejected fixtures by hash. Require the R52 gate alongside all prior gates without lowering thresholds or inventing independent review.